import React from 'react';
import {View, Text} from 'react-native';
export default function WebViewErrorView() {
    return (
        <View style={{justifyContent: 'center', alignItems: 'center'}}>
            <Text>WebView render error</Text>
        </View>
    );
}
